#include <cmath>

int main(int argc,char* arg[])
{
    int f =2, g=3 ;
    double x = 1.0, y = 1.0, u = 3.4, z,h;

    z = fmod(x,y);

    z++;

    h = (double(f))/(double(g));

    double array1[4] = {1,1,1,1};

    double array2[2][2] = {{1,0},{0,1}};

    return 0;
}